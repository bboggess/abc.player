A work-in-progress project to parse ABC files and play them back using MIDI synthesizer.

This project depends on Antlr4, but these dependencies are pulled in automatically as a part of the build process.
